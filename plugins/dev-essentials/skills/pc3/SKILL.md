---
name: pc3
description: Shortcut alias for /procon3 — find 3 alternatives with pros/cons
argument-hint: <statement or question>
---

**IMMEDIATE EXIT if no argument:**
- If `$ARGUMENTS` is empty: output "❌ Error: Topic required. Usage: /pc3 <decision or question>" and STOP.

Invoke the `procon3` skill with `$ARGUMENTS`.
